#include <iostream>
#include <src/perception/model.hpp>
#include <iostream>

int main() {
    // u2d::body_t b;
    // std::cerr << "sensor data size (quantum): " << sizeof(u2d::body_t) << " B" << std::endl;
    // std::cerr << "sensor data size (match)  : " << sizeof(u2d::body_t)*3000 << " B (" << sizeof(u2d::body_t)*3000/1024 << ") KB" << std::endl;
    // // Fill data:
    // b.sequence = 1;
    // b.view_mode = u2d::body_t::view_mode_t::low;
    // b.stamina.stamina = 1000;
    // b.stamina.effort = 10;
    // b.stamina.capacity = 1500;
    return 0;
}

